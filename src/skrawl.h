#ifndef SKRAWL_H
#define SKRAWL_H

// shared includes
#include <stdio.h>


// shared utility functions
static inline char* sc(char *s,char c){ while(*s!=c)if(!*s++)return (char*)0; return s; }

#endif